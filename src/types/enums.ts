export enum TransactionForwardStatus {
	WAITING = "waiting",
	NEEDS_EDITING = "needs-editing",
	REJECTED = "rejected",
	APPROVED = "approved",
}

export enum TransactionPriority {
	HIGH = "high",
	MEDIUM = "medium",
	LOW = "low",
}

export enum UserGroups {
	ADMIN = "admin",
	USER = "user",
}
