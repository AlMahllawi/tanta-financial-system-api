export enum TransactionForwardStatus {
	WAITING = "waiting",
	NEEDS_EDITING = "needs-editing",
	REJECTED = "rejected",
	APPROVED = "approved",
	FULFILLED = "fulfilled",
}

export enum TransactionPriority {
	HIGH = "high",
	MEDIUM = "medium",
	LOW = "low",
}
